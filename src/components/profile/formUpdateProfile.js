import { LoadingMessage } from "../utilities/LoadingMessage";

export const ProfileUpdate = () => {
  return (
    <div>      
      <h1 className="text-white text-center"> Formulario de actualizacion de Perfil </h1>    
      <LoadingMessage/>
    </div>    
  );
};